from cards import MO,NG,NR,SK,ST,SY,neutral
from functional.calculations import calculator
from functional.visualisation import visualise

visualise(calculator(MO))
visualise(calculator(NG))
visualise(calculator(NR))
visualise(calculator(SK))
visualise(calculator(ST))
visualise(calculator(SY))
visualise(calculator(neutral))
